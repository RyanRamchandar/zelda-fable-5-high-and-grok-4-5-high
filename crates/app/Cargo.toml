[package]
name = "app"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
engine = { path = "../engine" }
game = { path = "../game" }
content = { path = "../content" }
