[package]
name = "game"
version = "0.1.0"
edition = "2021"

[dependencies]
engine = { path = "../engine" }
content = { path = "../content" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
